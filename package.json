{
  "name": "2025-08-agent-mode-presentation",
  "version": "1.0.0",
  "description": "Presentation about Agentic Coding Tools and Agent Mode",
  "main": "index.js",
  "scripts": {
    "build": "marp presentation.md --allow-local-files -o dist/presentation.pptx",
    "build:editable": "marp presentation.md --pptx-editable --allow-local-files -o dist/presentation-editable.pptx",
    "build:all-formats": "marp presentation.md --allow-local-files --html -o dist/presentation.html && marp presentation.md --allow-local-files --pdf -o dist/presentation.pdf && marp presentation.md --allow-local-files --pptx -o dist/presentation.pptx",
    "dev": "marp --preview --allow-local-files presentation.md",
    "watch": "marp --watch --allow-local-files presentation.md -o dist/presentation.pptx",
    "server": "marp --server ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "marp",
    "presentation",
    "ai",
    "agent",
    "coding"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3"
  }
}
